version: '3.1'

services:

  db:
    image: postgres
    volumes:
    - ./db:/var/data
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
      PGDATA: /var/data
    ports:
      - 5432:5432